import 'package:flutter/material.dart';

class Height32Widget extends StatelessWidget {
  const Height32Widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 32,
    );
  }
}
